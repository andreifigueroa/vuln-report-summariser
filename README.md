# Vuln Report Summariser

Reads a vulnerability scan CSV export and generates a recruiter-friendly summary report.

## Usage

```bash
python main.py --input sample_scan.csv
```

Outputs `summary_report.md`.
